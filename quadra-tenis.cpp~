#include <bits/stdc++.h>

using namespace std;

int main()
{
	int coords[2];
	
	for(int i=0; i<2; i++)
		cin >> coords[i];
		
	if( coords[1] < 0 || coords[1] > 468 || coords[1] < 0 || coords[1] > 468)
		printf("fora\n");
	else
		printf("dentro\n");
	return 0;
}
